config:

- matcher:
    require:
    - 'agent.(1)product.(1)comments.entry{"402LG"~"Build"'
    extract:
    - 'DeviceBrand :   3000:"LG"'

- matcher:
    require:
    - 'agent.(1)product.(1)comments.entry{"AS98"~"Build"'
    extract:
    - 'DeviceBrand :   3000:"LG"'

- matcher:
    require:
    - 'agent.(1)product.(1)comments.entry{"RS98"~"Build"'
    extract:
    - 'DeviceBrand :   3000:"LG"'

- matcher:
    require:
    - 'agent.(1)product.(1)comments.entry{"DM-01"~"Build"'
    extract:
    - 'DeviceBrand :   3000:"LG"'

- matcher:
    require:
    - 'agent.(1)product.(1)comments.entry{"L-01"~"Build"'
    extract:
    - 'DeviceBrand :   3000:"LG"'

- matcher:
    require:
    - 'agent.(1)product.(1)comments.entry{"VK"~"Build"'
    extract:
    - 'DeviceBrand :   3000:"LG"'

- matcher:
    require:
    - 'agent.(1)product.(1)comments.entry{"VS"~"Build"'
    extract:
    - 'DeviceBrand :   3000:"LG"'
